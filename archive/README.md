# archive/ — Dead / Superseded Runtime Files

Files here are **retired runtime artifacts** (not research outputs). They are kept for
reference only and are safe to delete once their backup copies are confirmed.

| File | Origin | Notes |
|---|---|---|
| `mark_1_execution.log` | `mark 1/` | Old execution log from the original Mark-1 notebook runs (superseded by `Evaluation/` suite) |
| `mark_1_execution.pid` | `mark 1/` | Stale process-ID file from the same runs |

Full backup of everything in the repository (notebooks, code, docs, outputs, weights) lives
in `backup/` — see `PROGRESS.md` §9 for the inventory and `backup/MANIFEST.json` for hashes.
