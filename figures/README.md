# figures/

Static plots used in documentation and reports (EDA, augmentation, preprocessing, split/burden analysis, tumor heatmaps, etc.).

> Regenerable from the notebooks in `notebooks/` and `studies/`; 14 files / ~4.8 MB — kept for docs.
