# Regeneration Map — How to Recreate Deleted Heavy Outputs

> **Purpose**: Document *exactly which code file regenerates each heavy output* that was deleted during the 2026-08-12 storage-recovery pass (7.07 GB / 222 files). If you ever need an artifact back, run the listed notebook/script in the listed order — no code changes required. All 222 deleted files are also recoverable instantly from `backup/` (zip + SHA-256 in `backup/MANIFEST.json`).
>
> **Golden rule**: `csv/json/png` results are NEVER deleted. Only regenerable intermediate artifacts (`.npz` probability caches, superseded `.pth` checkpoints) were removed. The four **required input checkpoints** were kept in place.

---

## 1. What was deleted (summary)

| Group | Count | Size | What it is |
|---|---|---|---|
| `.npz` probability caches (all folders) | 164 | ~2,461 MB | per-slice forward-pass probability caches; recomputed by any notebook when missing |
| `models/s9_pilot*`, `s9_finetune_v4` | 40 | ~3,293 MB | superseded training checkpoints (early pilot/finetune runs) |
| `models/research_validation/` | 4 | ~329 MB | `research_baseline.yaml` / `research_faup_*` harness runs |
| `experiments/sprint1/` (sweep_w2/3/5) | 9 | ~741 MB | 1-epoch UWACL weight sweep checkpoints |
| `Practice/*_outputs/*_best.pth` (non-multitask) | 8 | ~659 MB | ablation-study checkpoints |
| `mark 1/mark_3_outputs/*.pth` + `mark_4_outputs/mark_4_last.pth` | 4 | ~111 MB | mark-3 overfit demos + superseded mark-4 "last" checkpoint |
| **Total** | **222** | **~7,073 MB** | all verified present in `backup/MANIFEST.json` before deletion |

---

## 2. Required input checkpoints — KEPT (never delete)

These are *loaded* by downstream code. Deleting them breaks every downstream notebook:

| Checkpoint | Path | Consumed by |
|---|---|---|
| `multitask_best.pth` | `Practice/multitask_liver_tumor_outputs/` | all `Evaluation/00–12`, `mark 1` notebooks, part-2 `step_03` + `step_15` |
| `mark_4_best.pth` | `mark 1/mark_4_outputs/` | part-2 steps 01/02/03/15; mark-4e fusion |
| `recall_loss_best.pth` | `mark 1/mark_4c_outputs/` (mirror in `Evaluation/output/06_mark_4c/data/`) | part-2 steps 02/15 (recall arm of fusion) |
| `two_channel_best.pth` | `mark 1/mark_4c_outputs/` (mirror in `Evaluation/output/06_mark_4c/data/`) | part-2 steps 02/15 (two-channel arm) |

> The `Evaluation/output` ↔ `mark 1/mark_*_outputs/` mirror (~1.36 GB) is intentionally kept: part-2 steps hardcode `mark 1/mark_*_outputs/` paths.

---

## 3. Regeneration recipes (by artifact group)

### 3.1 `.npz` probability caches — 164 files / ~2,461 MB

Regenerated automatically by any notebook that consumes them (the loader recomputes and rewrites the cache when the `.npz` is absent). No manual step needed.

| Where they live | What regenerates them | How |
|---|---|---|
| `Evaluation/output/*/caches/*.npz` | `Evaluation/00_pipeline_overview_and_setup.ipynb` → notebooks `01`–`12` | run in order (REUSE mode auto-recomputes missing caches) |
| `mark 1/mark_*_outputs/` | same `Evaluation/` notebooks (they mirror back to `mark 1/`) | run `Evaluation/` suite |
| `mark 1 (part 2)/step_02/step_04/step_16` | `mark 1 (part 2)/step_XX_*/step_XX_*.ipynb` | run each step notebook |
| `Practice/*_outputs/*.npz` | the matching `Practice/<study>.ipynb` | run that ablation notebook |

### 3.2 `models/s9_pilot*` (21 files) + `s9_finetune_v4` (12 files) — ~3,293 MB

Superseded training checkpoints. Regenerable via the finetune/pilot training notebooks:

| Checkpoints | Regenerating notebook |
|---|---|
| `models/s9_pilot/best_model.pth` etc. | `notebooks/05_Training_Pilot.ipynb` (or `studies/03_training_pilot.ipynb`) |
| `models/s9_pilot_v2/*` | `studies/03_training_pilot.ipynb` (variant config) |
| `models/s9_pilot_v3/*` | `tracking/_pilot_v3.py` → writes `models/s9_pilot_v3/` |
| `models/s9_finetune_v4/*` | `notebooks/06_Training_FineTune.ipynb` / `studies/04_training_finetune.ipynb`; `tracking/_eval_v3.py` reads `models/s9_finetune_v4` |

### 3.3 `models/research_validation/` (4 files) — ~329 MB

Regenerated by the research harness configs:

```
python tools/train.py --config configs/experiments/research_baseline.yaml
python tools/train.py --config configs/experiments/research_faup_focal.yaml
python tools/train.py --config configs/experiments/research_faup_uwacl.yaml
python tools/train.py --config configs/experiments/research_manifest_baseline.yaml
```
Consumed by `tools/analyze.py` and `tools/report.py`; procedure documented in `docs/MANUAL_RESEARCH_VALIDATION.md`.

### 3.4 `experiments/sprint1/` (sweep_w2/3/5) — 9 files / ~741 MB

Regenerated by the UWACL weight sweep:

```
python tracking/_sweep.py        # 1-epoch sweep across tumor_weights 2, 3, 5
```
or interactively via `notebooks/sprint1_sweep.ipynb`. Wrapper: `src/training/sweep_runner.py`.

### 3.5 `Practice/*_outputs/*_best.pth` (8 files, non-multitask) — ~659 MB

Each is the output of its own ablation notebook (regenerate by running the matching notebook):

| Deleted checkpoint | Regenerating notebook |
|---|---|
| `context_2_5d_outputs/patient_aware_best.pth` | `Practice/adjacent_slice_2_5d_context_ablation.ipynb` |
| `intensity_robustness_outputs/patient_aware_best.pth` | `Practice/organ_normalized_intensity_robustness_ablation.ipynb` |
| `manifest_baseline_smoke_outputs/baseline_smoke_best.pth` | `Practice/manifest_baseline_5epoch_smoke_test.ipynb` |
| `patient_aware_baseline_outputs/patient_aware_best.pth` | `Practice/baseline_25epoch_patient_aware_training.ipynb` |
| `patient_lesion_balanced_outputs/patient_aware_best.pth` | `Practice/patient_lesion_balanced_sampler_ablation.ipynb` |
| `recall_aware_loss_outputs/patient_aware_best.pth` | `Practice/recall_aware_focal_tversky_loss_ablation.ipynb` |
| `stabilized_composite_loss_outputs/patient_aware_best.pth` | `Practice/stabilized_composite_loss_ablation.ipynb` |
| `verified_loader_overfit_outputs/overfit_last_checkpoint.pth` | `Practice/verified_loader_overfit_...ipynb` (`create_verified_loader_overfit_notebook.py`) |

> **Do NOT delete** `Practice/multitask_liver_tumor_outputs/multitask_best.pth` — that is the source checkpoint (see §2).

### 3.6 `mark 1/mark_3_outputs/*.pth` (3) + `mark_4_outputs/mark_4_last.pth` (1) — ~111 MB

Regenerated by the canonical Evaluation suite (they mirror back into `mark 1/`):

| Deleted | Regenerating notebook |
|---|---|
| `mark 3` overfit checkpoints | `Evaluation/03_mark_3_two_stage_multiwindow_overfit.ipynb` |
| `mark_4_last.pth` | `Evaluation/04_mark_4_two_stage_validation_smoke.ipynb` |

### 3.7 step_13 raw extracted DICOM — `outputs/extracted/` (34,322 files / ~10.8 GB)

Removed 13 Aug 2026 after ingestion QC passed and step_14 consumed it. Downstream steps 15–21 read **step_14's normalized `.nii.gz`**, not this raw tree. The 20 patient ZIP archives remain in `outputs/raw_downloads/` (782 MB, sha-256 in `outputs/download_manifest.csv`, `testzip()`-verified). Re-extraction:

1. Open `mark 1 (part 2)/step_13_3d_ircadb_ingestion_and_qc/step_13.ipynb`.
2. Keep `DOWNLOAD_ENABLED = False` (zips already present), set `EXTRACT_ENABLED = True`.
3. **Restart Kernel and Run All** — deterministic `extractall`, verified against `outputs/extracted_file_inventory.csv` (sha-256 per file).

A stale incomplete download `outputs/raw_downloads/3Dircadb1.zip.part` (820 MB, absent from the manifest) was also removed.

### 3.8 step_14 normalized NIfTI — `outputs/normalized/` (60 files / ~923 MB)

Removed 14 Aug 2026 after the external evaluation (step_16) completed. Downstream evidence (Steps 17–21) reads only the csv/json/png/signature artifacts that remain in `outputs/`, so nothing depends on `outputs/normalized/`. The exact per-file SHA-256 is preserved in `outputs/conversion_manifest.csv` (`ct_sha256`, `liver_sha256`, `tumour_sha256`). Regeneration:

1. The source is the step_13 archive held in `mark 1 (part 2)/step_13_3d_ircadb_ingestion_and_qc/outputs/raw_downloads/` (20 verified patient ZIPs, sha-256 in `download_manifest.csv`).
2. Open `mark 1 (part 2)/step_14_3d_ircadb_normalized_conversion_and_parity_qc/step_14.ipynb` (no download needed — it reads the step_13 extraction).
3. **Restart Kernel and Run All** — deterministic conversion; every regenerated file must match the hashes in `conversion_manifest.csv`.
4. If the actual external-evaluation results are needed again, re-run Steps 15–16 after regeneration.

---

## 4. Canonical re-run order (full pipeline, from scratch)

1. **Source checkpoint**: `Practice/multitask_liver_tumor_outputs/multitask_best.pth` (kept).
2. **Evaluation suite** (regenerates all mark outputs + caches + mirrors): run `Evaluation/00_pipeline_overview_and_setup.ipynb` → `01` … `12` in order.
3. **Part-2 frozen steps**: `mark 1 (part 2)/step_XX_*/step_XX_*.ipynb` in step order 00 → 21.
4. **Practice ablations / research experiments**: only if you need those specific artifacts (§3.5 / §3.3 / §3.4).

---

## 5. Recovery from backup (instant, no re-run needed)

Every deleted file still exists in `backup/`:

```
python tools/backup_project.py          # re-runs backup if you ever want a fresh snapshot
```
- Look up any file by path in `backup/MANIFEST.json` → get its `zip` + `sha256`.
- Unzip that zip at the repo root; relative paths are preserved.
- All 7 remaining zips verified `testzip()`-clean on 2026-08-12. Note: the two heavy checkpoints/caches zips (`06_models_pth_all.zip`, `07_caches_npz_all.zip`) were removed 14 Aug 2026 — those files' full metadata (path/size/SHA-256) is in `docs/BACKUP_CHECKPOINT_CACHE_INVENTORY.md`, and their contents are regenerable via §3.

---

*Maintained as part of the 2026-08-12 storage-recovery pass. See `PROGRESS.md` §11 for the change log and `Evaluation/PROJECT_EVALUATION_DATA.md` for the full results data card.*
